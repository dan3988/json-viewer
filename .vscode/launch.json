{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Chrome",
			"type": "chrome",
			"userDataDir": "${workspaceFolder}\\.chrome",
			"request": "launch",
			"runtimeArgs": ["--load-extension=${workspaceFolder}"]
			//"url": "chrome://extensions/"
		}
	]
}